# Lazy Workspace Guard Suite

Installs the remote **Lazy Workspace Guard** core and the local **SSH Bridge** together. The core performs bounded directory reads on the connected Linux host; the bridge resolves local OpenSSH profiles into adapted `ssh` and file-download `scp` commands without reading private-key contents.
